import { SvgIcon } from "@mui/material";
import SupportIconSVG from "../../../public/icons/SupportIcon.svg";

const SupportIcon = (props: any) => {
  return <SvgIcon {...props} component={SupportIconSVG} />;
};

export default SupportIcon;
