import { SvgIcon } from "@mui/material";
import CopyIconSVG from "../../../public/icons/CopyIcon.svg";

const CopyIcon = (props: any) => {
  return <SvgIcon {...props} component={CopyIconSVG} />;
};

export default CopyIcon;
