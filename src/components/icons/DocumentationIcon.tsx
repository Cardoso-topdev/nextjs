import { SvgIcon } from "@mui/material";
import DocumentationIconSVG from "../../../public/icons/DocumentationIcon.svg";

const DocumentationIcon = (props: any) => {
  return <SvgIcon {...props} component={DocumentationIconSVG} />;
};

export default DocumentationIcon;
