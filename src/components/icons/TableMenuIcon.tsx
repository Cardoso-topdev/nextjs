import { SvgIcon } from "@mui/material";
import TableMenuIconSVG from "../../../public/icons/TableMenuButton.svg";

const TableMenuIcon = (props: any) => {
  return <SvgIcon {...props} component={TableMenuIconSVG} />;
};

export default TableMenuIcon;
