import { SvgIcon } from "@mui/material";
import LogoutIconSVG from "../../../public/icons/LogoutIcon.svg";

const LogoutIcon = (props: any) => {
  return <SvgIcon {...props} component={LogoutIconSVG} />;
};

export default LogoutIcon;
