import { SvgIcon } from "@mui/material";
import ProfileIconSVG from "../../../public/icons/ProfileIcon.svg";

const ProfileIcon = (props: any) => {
  return <SvgIcon {...props} component={ProfileIconSVG} />;
};

export default ProfileIcon;
