import { SvgIcon } from "@mui/material";
import EditIconSVG from "../../../public/icons/EditIcon.svg";

const EditIcon = (props: any) => {
  return <SvgIcon {...props} component={EditIconSVG} />;
};

export default EditIcon;
